import { createHash } from "node:crypto";
import { NextResponse } from "next/server";
import { pool } from "../../_lib/server";
import { rateLimit } from "../../_lib/security";
const hash=(value)=>createHash("sha256").update(value).digest("hex");
export async function GET(req){const key=req.headers.get("x-api-key");if(!key)return NextResponse.json({error:"Missing x-api-key"},{status:401});const keyHash=hash(key);const limit=rateLimit(`public-api:${keyHash}`,120,60_000);if(!limit.allowed)return NextResponse.json({error:"Rate limit exceeded"},{status:429,headers:{"Retry-After":String(limit.retryAfter)}});const auth=await pool.query("SELECT id,user_id FROM api_keys WHERE key_hash=$1 AND revoked_at IS NULL",[keyHash]);if(!auth.rows[0])return NextResponse.json({error:"Invalid API key"},{status:401});await pool.query("UPDATE api_keys SET last_used_at=NOW() WHERE id=$1",[auth.rows[0].id]);const requestUrl=new URL(req.url);const limitParam=Math.min(200,Math.max(1,Number.parseInt(requestUrl.searchParams.get("limit")||"50",10)||50));const offset=Math.max(0,Number.parseInt(requestUrl.searchParams.get("offset")||"0",10)||0);const r=await pool.query("SELECT t.id,t.name,t.workspace_id,t.created_at FROM tables t JOIN workspaces w ON w.id=t.workspace_id WHERE w.owner_id=$1 ORDER BY t.created_at DESC LIMIT $2 OFFSET $3",[auth.rows[0].user_id,limitParam,offset]);return NextResponse.json({data:r.rows,pagination:{limit:limitParam,offset,nextOffset:r.rows.length===limitParam?offset+limitParam:null}},{headers:{"Cache-Control":"private, no-store"}})}
