// Main Workspace Page removed
import React from 'react';
import TableBoard from '../TableBoard';

export default function MainWorkspacePage() {
  // You can pass props or context if needed
  return <TableBoard />;
}